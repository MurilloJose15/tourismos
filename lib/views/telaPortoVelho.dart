import 'package:flutter/material.dart';

class TelaPortoVelho extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Porto-Velho-RO'),
      ),
    );
  }
}
